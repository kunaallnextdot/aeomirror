fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.4
SQLAlchemy==2.0.31
beautifulsoup4==4.12.3
lxml==5.2.2
httpx==0.27.0
python-dotenv==1.0.1
# --- Phase 1: production infra (INTEGRATIONS.md A1/A2/A3) ---
psycopg[binary]==3.2.9     # PostgreSQL driver (psycopg 3). URL: postgresql+psycopg://
redis==5.0.7               # shared cache + rate-limit store
alembic==1.13.2            # database migrations (replaces create_all on startup)
pytest==8.2.2
# --- Phase 5: authentication + multi-user SaaS ---
PyJWT==2.8.0               # JWT access tokens (HS256)
bcrypt==4.1.3              # password hashing
email-validator==2.1.1     # pydantic EmailStr validation
# --- Phase 6: exportable AI-visibility reports ---
reportlab==4.2.2          # pure-Python PDF generation (no system libs)
openpyxl==3.1.2           # parse .xlsx uploads for bulk scans (first column)
python-multipart==0.0.9   # multipart/form-data parsing for bulk CSV/XLSX uploads
# --- AI features (Anthropic) — server-side only ---
# Powers the AI-written report narrative + AI content-quality insights. Both features
# degrade gracefully to the deterministic rule-based path when ANTHROPIC_API_KEY is
# unset, so this dependency is only exercised when AI is configured.
anthropic==0.40.0
# --- Phase 10: production (optional) ---
# Error tracking. Install only if you set SENTRY_DSN; the app runs fine without it.
# sentry-sdk==2.14.0
gunicorn==22.0.0          # production process manager for uvicorn workers
psycopg2-binary==2.9.9